---
import Greeting from "../components/Greeting.astro";
import PlaylistItemCard from "../components/PlaylistItemCard.astro";
import Layout from "../layouts/Layout.astro";
import { playlists } from "../lib/data";
---

<Layout title="Spotify clon">
  <div
    id="playlist-container"
    class="relative trnasition-all duration-1000 bg-green-600"
  >
    <div class="relative z-10 px-6 pt-10">
      <Greeting />
      <div
        class="grid grid-cols-2 sm:grid-cols-2 mt-6 gap-2 md:flex md:flex-wrap md:gap-4"
      >
        {
          playlists.map((playlists) => (
            <PlaylistItemCard playlist={playlists} />
          ))
        }
      </div>
    </div>

    <div
      class="absolute -z-9 inset-0 bg-gradient-to-t from-zinc-900 via-zinc-900/80"
    >
    </div>
  </div>
</Layout>

<style></style>
